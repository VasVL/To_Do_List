<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootLayout"
    android:background="@color/backPrimary"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:liftOnScrollTargetViewId="@id/recyclerView"
        android:fitsSystemWindows="true"
        android:background="@color/backPrimary"
        >

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsingToolbar"
            style="?attr/collapsingToolbarLayoutLargeStyle"
            android:layout_width="match_parent"
            android:layout_height="?attr/collapsingToolbarLayoutLargeSize"
            android:fitsSystemWindows="true"
            app:layout_scrollFlags="scroll|snap|exitUntilCollapsed"
            android:background="@color/backPrimary"
            app:titleCollapseMode="scale"
            app:expandedTitleMarginStart="@dimen/allDealsTitleMarginStart"
            app:expandedTitleMarginBottom="@dimen/allDealsTitleMarginBottom"
            app:expandedTitleTextAppearance="@style/ExpandedAppBar"
            >

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:title="@string/allDealsTitle"
                app:layout_collapseMode="pin"
                />

            <TextView
                android:id="@+id/textDone"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Выполнено - "
                android:textColor="@color/gray"
                android:textSize="@dimen/textSizeMid"
                android:lines="1"
                android:ellipsize="start"
                app:layout_collapseMode="parallax"
                app:layout_collapseParallaxMultiplier="0.5"
                android:layout_gravity="start|bottom"
                android:layout_marginStart="@dimen/allDealsTitleMarginStart"
                android:layout_marginBottom="@dimen/allDealsSubtitleMarginBottom"
                />

            <ImageView
                android:id="@+id/showAllDeals"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                app:layout_collapseMode="pin"
                android:layout_marginEnd="@dimen/toolbarMenuMarginEnd"
                android:layout_marginBottom="@dimen/toolbarMenuMarginBottom"
                android:src="@drawable/baseline_visibility_24"/>


        </com.google.android.material.appbar.CollapsingToolbarLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <FrameLayout
        android:id="@+id/shadowToolbarLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior"
        >

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:itemCount="10"
            tools:listitem="@layout/list_item_to_do" />

        <View android:id="@+id/toolbarShadow"
            android:layout_width="match_parent"
            android:layout_height="@dimen/toolbarShadowHeight"
            android:layout_gravity="top"
            android:background="@drawable/toolbar_dropshadow"/>

    </FrameLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/blue"
        app:fabSize="normal"
        app:layout_anchor="@id/shadowToolbarLayout"
        app:layout_anchorGravity="bottom|end"
        android:layout_marginEnd="@dimen/fabMarginEnd"
        android:layout_marginBottom="@dimen/fabMarginBottom"
        android:src="@drawable/baseline_add_24" />
<!--        app:hideMotionSpec=""-->

</androidx.coordinatorlayout.widget.CoordinatorLayout>